<!DOCTYPE html>
<html lang="en">
{{> fragments/html_header}}

<body>

    {{> fragments/top_bar}}

    <a href="/professor" class="join-class-button back">back to course catalog</a>
    <div class="spacer"></div>
    <div class="spacer"></div>

    <div class="spacer"></div>
    <label class="subtitle-bar create-class"> create a new class </label>
     <div class="spacer"></div>
    <label class="subtitle-bar warning"> WARNING: you can only create a new class if you have a valid professor account. </label>
     <div class="spacer"></div>
    <div class="spacer"></div>

    <!-- Modify the form action to match the route in the CreateClassController -->
    <form method="post" action="/professor/create-a-class/newclass">
        <!-- Add input fields for courseId, courseName, numGroups, and groupSize -->
        <label for="crn" class="form-label">CRN:</label>
        <input type="text" name="courseId" class="short-input" placeholder="Course ID">

        <label for="name" class="form-label">Course Name:</label>
        <input type="text" name="courseName" class="short-input" placeholder="Course Name">

        <label for="numGroups" class="form-label">Number of Groups:</label>
        <input type="text" name="numGroups" class="short-input" placeholder="Number of Groups">

        <label for="groupSize" class="form-label">Group Size:</label>
        <input type="text" name="groupSize" class="short-input" placeholder="Group Size">

        <button type="submit" class="login-action-button create-class">Create Class</button>
    </form>

    {{> fragments/footer}}

</body>

</html>
